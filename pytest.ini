[pytest]
testpaths = tests
markers =
    smoke: quick, high-priority checks
    regression: broader functional coverage

addopts =
    --html=reports/report.html
    --self-contained-html
    --junitxml=reports/junit.xml
    --tracing=retain-on-failure
    --screenshot=only-on-failure
    --video=retain-on-failure